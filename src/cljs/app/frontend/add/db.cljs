(ns app.frontend.add.db
  ;; (:require  [app.db :as db]
  ;;            [next.jdbc :as jdbc]
  ;;            [re-frame.core :as rf])
  )

;; (rf/reg-event-db
;;  :add/add-task
;;  (fn [db [_ {:keys [result]}]]
;;    (assoc db :add/result result)))
